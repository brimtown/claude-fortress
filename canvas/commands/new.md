---
description: Strike the earth! Embark on a new fortress.
---

Activate the claude-fortress skill and spawn a new fortress immediately. Generate a dramatic dwarven name and embark!

If the user provides a name in $ARGUMENTS, use that as the fortress name. Otherwise generate one.

Always use `"save":true` to enable autosave.
